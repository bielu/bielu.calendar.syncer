{
  "name": "bielu-calendar-syncer",
  "version": "1.1.0",
  "private": true,
  "description": "Root workspace for changeset-driven versioning of the Bielu.Calendar.Syncer NuGet suite. Not published. Every NuGet package in this repository shares the single version recorded here and in version.props.",
  "scripts": {
    "changeset": "changeset",
    "version": "changeset version && node scripts/apply-nuget-version.mjs",
    "changeset:status": "changeset status --since=origin/main"
  },
  "devDependencies": {
    "@changesets/changelog-github": "^0.5.0",
    "@changesets/cli": "^2.27.0"
  }
}
